# Core
torch>=2.0
transformers>=5.14.0
scipy
huggingface_hub

# vivekvar/turboquant, installed from the actual source that matches the
# TurboQuantCache(model.config, nbits=..., skip_layers=...) API -- NOT
# `pip install turboquant` (PyPI has a different, incompatible v0.2.0 API
# under the same name; see README.md's "Read this first" section).
# git clone https://huggingface.co/vivekvar/turboquant turboquant_src
# pip install -e ./turboquant_src

# Needed only for Part 2 (run_one_mlx_config.py / evaluate_mlx_backend.py --
# mlx-vlm's own backend + real TurboQuant, for comparison against Part 1).
# mlx-vlm itself: pip install -e /path/to/mlx-vlm (see ../whisper_turboquant/)
mlx>=0.32.2

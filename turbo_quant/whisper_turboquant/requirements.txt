# Core
mlx>=0.32.2
mlx-whisper>=0.5.0
numpy
safetensors
huggingface_hub

# Used by run_one_config.py / evaluate.py to load and resample audio to
# 16kHz mono without depending on ffmpeg being on PATH.
soundfile
scipy

# Needed only to import mlx_vlm.turboquant / mlx_vlm.models.* for the
# TurboQuant KV-cache scripts (turboquant_decoder.py, run_one_config.py).
# Install mlx-vlm itself from the cloned repo:
#   git clone git@github.com:Blaizzy/mlx-vlm.git
#   pip install -e ./mlx-vlm
# (its own requirements.txt pulls in transformers, Pillow, etc.)

# Optional: only needed if you use mlx_whisper.transcribe directly on
# arbitrary audio formats (mp3/flac/etc.) instead of via run_one_config.py's
# soundfile-based loader.
# brew install ffmpeg

# Needed only for run_faster_whisper_config.py / evaluate.py --include-faster-whisper
# (the faster-whisper / CTranslate2 comparison baseline).
faster-whisper
